# ============================================================================
# RAG System Evaluation - Requirements
# ============================================================================
# 
# TA Assistant RAG 시스템 평가를 위한 모든 의존성
# 
# 전체 설치:  pip install -r requirements.txt
# 최소 설치:  pip install -r requirements.txt --only-binary :all:
# 
# ============================================================================

# ----------------------------------------------------------------------------
# 1. LLM-as-Judge API (evaluate_rag.py)
# ----------------------------------------------------------------------------
openai>=1.0.0                  # OpenAI API (gpt-4o-mini 권장)
anthropic>=0.18.0              # Anthropic API (claude-3-haiku) - 대안

# ----------------------------------------------------------------------------
# 2. LangChain Core (evaluate_vectorstore.py, evaluate_rag.py)
# ----------------------------------------------------------------------------
langchain-core>=0.1.0          # 핵심 추상화 (Document, Retriever)
langchain-community>=0.0.20    # 커뮤니티 통합 (DuckDuckGo 등)
langchain-text-splitters>=0.0.1  # 텍스트 분할기 (Recursive, Token)
langchain-chroma>=0.1.0        # ChromaDB 벡터스토어 통합
langchain-huggingface>=0.0.1   # HuggingFace 임베딩 통합

# ----------------------------------------------------------------------------
# 3. Vector Store (evaluate_vectorstore.py)
# ----------------------------------------------------------------------------
chromadb>=0.4.0                # ChromaDB 벡터 데이터베이스

# ----------------------------------------------------------------------------
# 4. Embeddings & ML (evaluate_vectorstore.py)
# ----------------------------------------------------------------------------
transformers>=4.35.0           # HuggingFace Transformers
sentence-transformers>=2.2.0   # Sentence Transformers (경량 임베딩)
torch>=2.0.0                   # PyTorch

# ----------------------------------------------------------------------------
# 5. 시각화 & 분석 (analyze_results.py)
# ----------------------------------------------------------------------------
matplotlib>=3.7.0              # 차트 생성
pandas>=2.0.0                  # 데이터 분석, CSV
numpy>=1.24.0                  # 수치 계산

# ----------------------------------------------------------------------------
# 6. 문서 로딩 (evaluate_vectorstore.py - TA 시스템 연동 시)
# ----------------------------------------------------------------------------
pypdf>=3.0.0                   # PDF 로딩
python-pptx>=0.6.21            # PPTX 로딩
docx2txt>=0.8                  # DOCX 로딩

# ----------------------------------------------------------------------------
# 7. 유틸리티
# ----------------------------------------------------------------------------
tqdm>=4.65.0                   # 진행 표시줄
python-dotenv>=1.0.0           # 환경 변수 (.env 파일)
click>=8.0.0                   # CLI 인터페이스

# ----------------------------------------------------------------------------
# 8. 웹 검색 (선택사항 - TA 시스템 web search fallback)
# ----------------------------------------------------------------------------
duckduckgo-search>=4.0.0       # DuckDuckGo 검색

# ============================================================================
# 선택적 의존성 (필요 시 주석 해제)
# ============================================================================

# 양자화 모델 지원 (대용량 LLM 실행 시)
# auto-gptq>=0.6.0             # GPTQ 양자화
# bitsandbytes>=0.41.0         # 4bit/8bit 양자화
# llama-cpp-python>=0.2.0      # GGUF 모델

# 고급 문서 처리
# unstructured>=0.10.0         # 다양한 문서 형식

# 개발/테스트
# pytest>=7.0.0                # 단위 테스트
# black>=23.0.0                # 코드 포맷팅